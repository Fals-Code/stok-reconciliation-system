import { randomUUID } from "node:crypto";
import Link from "next/link";
import { archiveProductBatchAction, blockProductBatchAction, reactivateProductBatchAction, unblockProductBatchAction, updateProductBatchAction } from "@/app/products/actions";
import { getProductBatchMasterData } from "@/lib/supabase-rest";
export const dynamic="force-dynamic";
type Q={success?:string;error?:string};
function Feedback({q}:{q:Q}){const m=q.success??q.error;return m?<p role="status" className={`mt-4 rounded-xl border p-3 ${q.error?"border-rose-400/30 text-rose-100":"border-emerald-400/30 text-emerald-100"}`}>{m}</p>:null}
function Id({batch,productId}:{batch:{batch_id:string;row_version:number};productId:string}){return <><input type="hidden" name="intentId" value={randomUUID()}/><input type="hidden" name="productId" value={productId}/><input type="hidden" name="batchId" value={batch.batch_id}/><input type="hidden" name="rowVersion" value={batch.row_version}/></>}
export default async function BatchPage({params,searchParams}:{params:Promise<{productId:string;batchId:string}>;searchParams:Promise<Q>}){const [{productId,batchId},q]=await Promise.all([params,searchParams]);const data=await getProductBatchMasterData();const batch=data.batches.find((r)=>r.batch_id===batchId&&r.product_id===productId);if(!batch)return <main className="p-8 text-slate-100">Batch tidak ditemukan.</main>;const audits=data.audits.filter((a)=>a.batch_id===batchId);const standard=batch.batch_kind_code==="STANDARD";const expired=batch.is_effectively_expired;const editable=standard&&batch.lifecycle_status_code!=="ARCHIVED";const fefoReason=batch.is_fefo_eligible?"ACTIVE, belum expired, saldo sellable dan available tersedia.":batch.lifecycle_status_code!=="ACTIVE"?`Lifecycle ${batch.lifecycle_status_code}.`:expired?"Efektif kedaluwarsa.":!batch.product_is_active?"Produk diarsipkan.":batch.available_qty<=0?"Available produk tidak tersedia.":"Safety buffer atau saldo sellable belum memenuhi.";return <main className="mx-auto max-w-5xl px-5 py-8 text-slate-100"><Link href={`/products/${productId}`} className="nav-link">Kembali ke Produk</Link><section className="panel-card mt-5"><p className="section-kicker">Batch {batch.batch_kind_code}</p><h1 className="section-title">{batch.batch_code}</h1><p>{batch.product_sku} — {batch.product_name}</p><Feedback q={q}/><div className="mt-5 grid gap-3 sm:grid-cols-3"><p>Status: <strong>{batch.lifecycle_status_code}</strong></p><p>Effective expiry: <strong>{batch.effective_expiry_state}</strong></p><p>FEFO: <strong>{batch.is_fefo_eligible?"Eligible":"Tidak eligible"}</strong></p><p>Manufactured: {batch.manufactured_date??"-"}</p><p>Expiry: {batch.expiry_date}</p><p>First received: {batch.received_first_at??"-"}</p><p>SELLABLE: {batch.sellable_qty}</p><p>QUARANTINE: {batch.quarantine_qty}</p><p>DAMAGED: {batch.damaged_qty}</p><p>Reserved: {batch.reserved_qty} (product-scoped)</p><p>Available: {batch.available_qty}</p><p>History: {batch.has_authoritative_history?"Ada":"Belum"}</p></div><p className="mt-4 text-sm text-slate-400">Alasan FEFO: {fefoReason} QUARANTINE adalah bucket stok, bukan status Batch. Master mutation tidak mengubah stok atau snapshot ledger lama.</p></section>{!standard?<section className="panel-card mt-5 text-amber-100">Batch {batch.batch_kind_code} dibuat domain retur dan hanya dapat dibaca pada workflow Admin STANDARD.</section>:<><section className="panel-card mt-5"><p className="section-kicker">Edit Batch</p>{!editable?<p className="mt-3 text-amber-100">Batch ARCHIVED tidak dapat diedit. Gunakan reaktivasi bila invariant tetap valid.</p>:<form action={updateProductBatchAction} className="form-grid mt-4"><Id batch={batch} productId={productId}/><label className="field-label">Kode Batch<input required name="batchCode" defaultValue={batch.batch_code}/></label><label className="field-label">Kind<input value="STANDARD" readOnly/></label><label className="field-label">Manufactured<input name="manufacturedDate" type="date" defaultValue={batch.manufactured_date??""}/></label><label className="field-label">Expiry<input required name="expiryDate" type="date" defaultValue={batch.expiry_date}/></label><label className="field-label">First received<input name="receivedFirstAt" type="datetime-local" defaultValue={batch.received_first_at?.slice(0,16)??""}/></label>{batch.has_authoritative_history&&<label className="field-label">Alasan koreksi expiry<textarea name="reason" required placeholder="Wajib bila expiry diubah setelah ada histori"/></label>}<label className="field-label">Catatan<textarea name="note"/></label><div><button className="primary-button">Simpan Batch</button></div></form>}</section><section className="panel-card mt-5"><p className="section-kicker">Lifecycle</p>{batch.lifecycle_status_code==="ACTIVE"&&<div className="grid gap-4 sm:grid-cols-2"><form action={blockProductBatchAction}><Id batch={batch} productId={productId}/><label className="field-label">Alasan block<textarea required name="reason"/></label><button className="primary-button mt-3">Block Batch</button></form><form action={archiveProductBatchAction}><Id batch={batch} productId={productId}/><label className="field-label">Alasan archive<textarea required name="reason"/></label><label><input type="checkbox" name="confirmation"/> Konfirmasi archive</label><button className="primary-button mt-3">Archive Batch</button></form></div>}{batch.lifecycle_status_code==="BLOCKED"&&<div className="grid gap-4 sm:grid-cols-2">{expired?<p className="text-amber-100">Batch efektif expired tidak dapat di-unblock menjadi ACTIVE.</p>:<form action={unblockProductBatchAction}><Id batch={batch} productId={productId}/><label className="field-label">Alasan unblock<textarea required name="reason"/></label><button className="primary-button mt-3">Unblock Batch</button></form>}<form action={archiveProductBatchAction}><Id batch={batch} productId={productId}/><label className="field-label">Alasan archive<textarea required name="reason"/></label><label><input type="checkbox" name="confirmation"/> Konfirmasi archive</label><button className="primary-button mt-3">Archive Batch</button></form></div>}{batch.lifecycle_status_code==="ARCHIVED"&&(expired?<p className="text-amber-100">Batch efektif expired tidak dapat direactivate.</p>:<form action={reactivateProductBatchAction}><Id batch={batch} productId={productId}/><label className="field-label">Alasan reaktivasi<textarea required name="reason"/></label><label><input type="checkbox" name="confirmation"/> Konfirmasi reaktivasi</label><button className="primary-button mt-3">Reactivate Batch</button></form>)}</section></>}<section className="panel-card mt-5"><h2 className="section-title">Audit Batch</h2>{audits.length?audits.map((a)=><article key={a.audit_id} className="mt-3 border-t border-white/10 pt-3 text-sm"><strong>{a.action_code}</strong> — {a.reason??a.note??"-"}<p className="text-slate-400">{a.actor_display_name??a.process_name??"Proses tepercaya"} · {a.occurred_at}</p>{a.before_snapshot&&a.after_snapshot&&<p className="text-slate-400">Before/after snapshot tersimpan untuk audit.</p>}</article>):<p className="mt-3">Belum ada audit.</p>}</section></main>}